fn main() {
    // Exemplos de tipos "numéricos"
    let inteiro = 5;
    let inteiroGrande = 199_254_5;
    let hexadecimal = 0xFF;
    let octadecimal = 0o77;
    let binario = 0b1111_0000;
    let byte = b'A';

    // Exemplo de tipo: (flutuante)
    let flutuante: f64 = 42.1;

    // Exemplo de tipo: (booleano)
    let booleano: bool = true;

    // Exemplo de tipo: (caractere)
    let letra: char = 'g';
    let emoji = '🚀';
}